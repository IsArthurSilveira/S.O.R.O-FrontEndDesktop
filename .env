# Configuração da URL base da API para o cliente de API gerado (AuthService, etc.)
# O valor deve ser a RAIZ do servidor, sem o prefixo /api/v1, pois o Swagger já o inclui.

VITE_API_URL=https://api-bombeiros-s-o-r-o.onrender.com

# URL do servidor de desenvolvimento local (opcional, mas útil para testes)
# VITE_API_URL_LOCAL=http://localhost:3000